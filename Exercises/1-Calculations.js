
function Calculations(x, y) {
    console.log(
          `Sum: ` + (x + y) + `\n`
        + `Sub: ` + (x - y) + `\n` 
        + `Mul: ` + (x * y) + `\n`
        + `Div: ` + (x / y)
        );
}

const number = new Calculations(1, 2);
