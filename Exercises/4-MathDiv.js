function mathDiv(dividend, divider) {   
    return console.log(
        `${dividend} divided by ${divider} is: ` + (dividend / divider) 
        + `\n`
        + `Remainder: ` + (dividend % divider)
        );    
}

const parameter  = new mathDiv(9,2);