
function Calculation (x, y) {
    console.log(
          `Sum: `, x + y, `\n`
        + `Sub: `, x - y, `\n` 
        + `Mul: `, x * y, `\n`
        + `Div: `, x / y
        );
}

const number = new Calculation(1, 2);
